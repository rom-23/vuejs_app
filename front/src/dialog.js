const Dialog = {
  confirmDialog(title, message, options) {
    console.log(title, message, options);
  },
  alertDialog(title, message, options) {
    console.log(title, message, options);
  },
};
module.exports = Dialog;
